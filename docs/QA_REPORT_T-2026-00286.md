# QA_REPORT_T-2026-00286 — AutoFlow CN F2 钉钉与企业微信 Piece MVP

## 部署验证

- QA 任务: T-2026-00292
- 开发任务: T-2026-00286
- 项目: P-2026-00036 AutoFlow CN
- PR: https://github.com/jinggo-company/project-autoflow-cn/pull/2
- 独立拉取: 已执行 `git fetch origin pull/2/head:qa-pr-2 --force`，本地 QA 分支 `qa-pr-2` 指向 `8e127943f8dcc753d6bc005bce1134df68528693`。
- PR 状态: `OPEN`，base `main`，head `feat/T-2026-00286-dingtalk-wecom-pieces`，GitHub 返回 `MERGEABLE`。
- 部署健康检查: 已执行 `docker compose up -d && sleep 30 && curl -s http://localhost:3000/api/v1/health | grep -o ok`，输出 `ok`，容器 `autoflow-cn-activepieces` 为 Running。
- 指定 test_command: 已执行 `pnpm test -- --runInBand && pnpm run test:pieces -- --filter dingtalk --filter wechat-work`，Vitest 主测试 2 个文件 6 个用例 PASS，Piece 专项 1 个文件 3 个用例 PASS。
- 构建验证: 已执行 `NODE_ENV=test pnpm build`，TypeScript 编译通过。

## AC覆盖

| 覆盖项 | 来源 | QA 复核结果 |
|---|---|---|
| AC-1 | `docs/PRD.md` F1 Docker Compose 一键启动 | PASS。F2 未破坏 F1 部署契约，Compose 启动后 health 可访问。 |
| AC-2 | `docs/PRD.md` F1 health 返回 ok | PASS。`curl -s http://localhost:3000/api/v1/health | grep -o ok` 输出 `ok`。 |
| AC-3 | `docs/PRD.md` F1 Webhook HTTP 工作流 | PASS。主测试仍覆盖创建、触发、查询工作流并返回 `SUCCEEDED`。 |
| AC-4 | `docs/PRD.md` F1 Builder 中文文案 | PASS。主测试仍覆盖 `/api/v1/i18n/builder` 中文文案。 |
| AC-5 | `docs/PRD.md` F1 中文错误消息 | PASS。主测试仍覆盖不存在工作流触发返回 `WORKFLOW_NOT_FOUND` 与中文错误消息。 |
| Gate dev_work ③ | T-2026-00286 钉钉 Piece MVP | PASS。TC-004 可复现，Webhook Trigger 触发钉钉 `sendTextMessage` Action，mock 收到 `x-autoflow-piece: dingtalk` 和钉钉文本消息 JSON。 |
| Gate dev_work ④ | T-2026-00286 企业微信 Piece MVP | PASS。TC-005 可复现，Webhook Trigger 触发企业微信 `sendTextMessage` Action，mock 收到 `x-autoflow-piece: wechat-work` 和企微群机器人文本消息 JSON。 |
| Piece Load | T-2026-00286 verification_actions | PASS。`/api/v1/pieces` 返回 `dingtalk` 与 `wechat-work`，二者均包含 `sendTextMessage` Action。 |

补充说明: 当前 `docs/PRD.md` 仍为 F1 PRD，正式 AC 仅列出 AC-1 到 AC-5；F2 的业务验收范围在任务描述、Gate G-2026-00091 dev_work ③/④、`docs/TEST_CASES.md` TC-004/TC-005 和 `docs/TEST_REPORT.md` 中定义。本 QA 已同时覆盖 PRD AC 与 F2 Gate 验收点。

## 破坏性测试

执行 mock provider 返回错误的负向测试：先创建钉钉 Piece 工作流，mock provider 对 `/mock/provider-error` 返回 HTTP 500，然后触发工作流。

结果如下：

```json
{
  "createStatus": 201,
  "triggerStatus": 202,
  "executionStatus": "FAILED",
  "diagnosticMessage": "钉钉消息发送失败：500"
}
```

结论: PASS。外部 mock provider 返回错误时，工作流不会误报成功，执行状态为 `FAILED`，且输出中文可诊断错误 `钉钉消息发送失败：500`，可以定位到钉钉消息发送动作的外部 HTTP 失败。

## 外部依赖状态

- 钉钉/企业微信真实开放平台凭证: 未使用。F2 MVP 明确允许使用本地 mock HTTP server，当前未验证真实 token、签名、审批流回调、客户联系事件或真实群机器人 URL。
- mock HTTP server: 已覆盖钉钉/企业微信机器人 Webhook 的请求头、JSON 请求体、HTTP 200 成功响应、HTTP 500 失败响应与工作流执行状态。
- GitHub Actions/CI: PR #2 仓库未配置 Actions checks；本次 QA 以本地指定 test_command、构建、Docker Compose 健康检查作为验收依据。
- Activepieces 上游: 当前为 F1/F2 本地兼容壳，不是完整上游 Activepieces 运行时；Piece 名称、Action 名称和 API 契约已在文档中固定，后续接入上游时需保持兼容。

## 文档复核

| 文件 | 复核结果 |
|---|---|
| `README.md` | PASS。列出 F2 Piece 验证接口、mock 覆盖边界与测试命令。 |
| `docs/TECH_STACK.md` | PASS。补充 F2 实现栈、兼容边界、mock 与 Node fetch 说明。 |
| `docs/ARCHITECTURE.md` | PASS。补充 `src/cn-pieces.ts`、Piece 工作流执行器、mock 范围和后续真实凭证边界。 |
| `docs/TEST_CASES.md` | PASS。TC-004/TC-005 包含 mock 契约、步骤、预期结果。 |
| `docs/TEST_REPORT.md` | PASS。记录 TC-004/TC-005 与 Piece Load 均 PASS，并说明 mock 覆盖范围。 |

## 结论

QA 结论: PASS。

T-2026-00286 在 PR #2 中完成钉钉与企业微信 Piece MVP，指定 test_command、TC-004、TC-005、Piece Load、Docker Compose health 和破坏性测试均通过。外部真实平台凭证未接入属于已声明范围限制，不阻塞 F2 MVP 验收；建议后续取得钉钉/企业微信测试应用或群机器人凭证后，沿用当前 Action 契约补充真实沙箱验收。
