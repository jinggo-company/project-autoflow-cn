# QA_REPORT_T-2026-00285

## 部署验证

- QA 任务：T-2026-00291
- 开发任务：T-2026-00285
- PR：<https://github.com/jinggo-company/project-autoflow-cn/pull/1>
- 验收分支：`qa/pr-1-head-T-2026-00291`
- 验收提交：`921e9dc6f166ebcd6fd47166c909159b2313152d`
- 验收时间：2026-06-10 Asia/Shanghai

已独立拉取 PR #1/head 到本地验收分支，并复核以下交付文档：`README.md`、`docs/PRD.md`、`docs/TECH_STACK.md`、`docs/ARCHITECTURE.md`、`docs/TEST_CASES.md`、`docs/TEST_REPORT.md`。

执行 T-2026-00285 指定 test_command：

```bash
cd /mnt/d/openworkspace/jinggo-company/company-repos/project-autoflow-cn && docker compose up -d && sleep 30 && curl -s http://localhost:3000/api/v1/health | grep -o ok && pnpm test -- --runInBand
```

结果：PASS。Docker Compose 成功启动 `autoflow-cn-activepieces`，端口 `3000:3000` 暴露正常；健康检查输出 `ok`；Vitest `src/__tests__/f1.spec.ts` 3 个测试全部通过。容器状态复核为 `Up ... (healthy)`，`docker inspect` 健康状态为 `healthy`。

本次未遇到 Docker 构建失败、端口占用或依赖下载失败。为避免历史容器影响，QA 执行前先运行了 `docker compose down --remove-orphans` 清理同项目容器。

## AC覆盖

| Case | 覆盖 AC | 对应验收点 | QA 复现方式 | 结果 |
|---|---|---|---|---|
| TC-001 | AC-1, AC-2 | Activepieces Docker Compose 启动验证，API health 返回 ok | `docker compose up -d` 后访问 `GET /api/v1/health`，响应包含 `status: "ok"`，并通过指定 test_command 的 `grep -o ok` | PASS |
| TC-002 | AC-3 | Webhook Trigger + HTTP Request Action 工作流创建、触发、查询 | 手动调用 `POST /api/v1/workflows/webhook-http` 创建工作流，再调用 `POST /api/v1/workflows/{id}/trigger`，最后 `GET /api/v1/executions/{id}` 查询 | PASS |
| TC-003 | AC-4, AC-5 | Builder 关键界面文本和错误消息中文化 | `GET /api/v1/i18n/builder` 返回 `工作流构建器`、`选择触发器`、`发布工作流`、`未找到工作流` 等中文文案；异常工作流触发返回中文错误 | PASS |

TC-002 手动复现证据：创建工作流返回 `trigger.type=WEBHOOK`、`action.type=HTTP_REQUEST`；触发后执行记录 `status=SUCCEEDED`；查询同一 execution 仍为 `SUCCEEDED`。

文档覆盖检查：PRD 定义 AC-1 到 AC-5；README 说明本地运行和 F1 验证接口；TECH_STACK 记录 F1 本地部署与中文化实现栈；ARCHITECTURE 记录 `src/` 兼容壳模块边界；TEST_CASES 覆盖 TC-001/TC-002/TC-003 并映射 AC-1 到 AC-5；TEST_REPORT 有开发自测 PASS 记录。QA 本次没有发现文档与实际接口不一致的问题。

## 破坏性测试

执行异常工作流触发：

```bash
curl -s -X POST http://localhost:3000/api/v1/workflows/not-found/trigger \
  -H 'content-type: application/json' \
  -d '{}'
```

实际响应：

```json
{"code":"WORKFLOW_NOT_FOUND","message":"未找到工作流"}
```

结论：PASS。异常工作流触发返回 404，并返回中文错误消息，满足 T-2026-00291 要求的“错误路由或异常工作流触发必须返回中文错误”。

补充观察：错误路由 `GET /api/v1/no-such-route` 当前返回 Fastify 默认英文 `Route GET:/api/v1/no-such-route not found`。本项不是本次必需通过项，因为 QA 要求允许在“错误路由”与“异常工作流触发”中择一覆盖；建议后续中文化全局 404 handler，使错误路由也统一中文返回。

## 外部依赖状态

- Docker / Docker Compose：可用；本地镜像构建和容器启动成功。
- Node / pnpm：仓库已有依赖可用，`pnpm test -- --runInBand` 成功执行。
- HTTP mock action：本任务使用 `mock://autoflow-cn/success`，不依赖外部公网 HTTP 服务。
- PostgreSQL / Redis / 上游 Activepieces：F1 当前交付为 Activepieces 兼容本地验证壳，按文档说明使用内存存储，不要求连接 PostgreSQL、Redis 或上游 Activepieces 0.48.x 镜像。
- GitHub CI：开发任务记录为未配置 Actions；本次 QA 以本地指定命令和手动接口复现为准。

## 结论

QA 结论：业务验收通过；qa_acceptance 结算资料缺口已补齐，待重新结算。

T-2026-00285 的 F1 验收范围已独立复现并覆盖 PRD AC-1 到 AC-5：Docker Compose 一键启动正常，健康检查返回 ok，Webhook Trigger + HTTP Request Action 示例工作流执行状态为 `SUCCEEDED`，Builder 关键界面文本和异常错误消息已中文化。破坏性测试覆盖异常工作流触发并返回中文错误。

补齐说明：`docs/PRD.md` 已补充 AC-1 到 AC-5；`docs/TEST_CASES.md`、`docs/TEST_REPORT.md`、`docs/QA_REPORT_T-2026-00285.md` 已更新 AC 覆盖引用，供 `company-os/scripts/check-qa-ac-coverage.sh T-2026-00291` 读取并校验。
